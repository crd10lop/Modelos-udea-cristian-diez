# Modelos 1 y 2 udea. IA y aprendizaje automatico

from local.lib.rlxmoocapi import submit, session
import inspect

session.LoginSequence(endpoint=init.endpoint, course_id=init.course_id, session_id="udea.vir.20262.g2", lab_id="L01.02", varname="student");
